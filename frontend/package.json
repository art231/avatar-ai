{
  "name": "avatar-ai-frontend",
  "version": "1.0.0",
  "description": "AvatarAI Frontend - Simple static application",
  "scripts": {
    "build": "mkdir -p dist && cp -r src/* dist/ 2>/dev/null || true && cp index.html dist/ 2>/dev/null || true",
    "start": "echo 'Build completed'"
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {}
}
